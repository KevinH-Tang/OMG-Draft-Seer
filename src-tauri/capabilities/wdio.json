{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Test-only WebDriver capability for the main window and Tauri overlay windows.",
  "windows": ["main", "overlay-*"],
  "permissions": [
    "core:default",
    "autostart:default",
    "wdio:default",
    "wdio-webdriver:default"
  ]
}
