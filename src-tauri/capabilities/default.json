{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal desktop capability for the main application window.",
  "windows": ["main"],
  "permissions": ["core:default", "wdio:default", "wdio-webdriver:default"]
}
