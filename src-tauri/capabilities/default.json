{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Production desktop capability for the main window and Tauri overlay windows.",
  "windows": ["main", "overlay-*"],
  "permissions": ["core:default", "autostart:default"]
}
